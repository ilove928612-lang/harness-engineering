# {{PROJECT_NAME}} — Agent 工作入口

> 本文件是目录页，不是百科全书。智能体从这里开始，按链接渐进式获取上下文。
> 由 harness-init 生成（六大概念：仓库即记录系统 / 地图而非手册 / 机械化执行 /
> 智能体可读性 / 吞吐量改变合并理念 / 熵管理）。

## 工作循环（每个任务必走）

1. 读本文件 + 相关目录的 AGENTS.md（如果有）
2. 实现：小步提交，一个逻辑变更一个 commit
3. 校验：`bash scripts/check-harness.sh` 必须全绿
4. 提交信息写明动机；测试偶发失败直接重跑（纠错成本低，等待成本高）

## 仓库结构

| 路径 | 内容 | 何时看 |
|------|------|--------|
| `{{STRUCTURE_ROWS}}` |  |  |

## 硬性规则（机械化执行守护，别手软）

- 不在仓库里的东西对智能体不存在：决策、规范、计划写成文件进仓库
- 所有路径引用必须真实存在（check.sh 校验，死链接 = FAIL）
- 所有 markdown 表格列数与表头一致（check.sh 校验）
- 不引入未在依赖清单声明的依赖
- 优先"无聊"技术：API 稳定、训练集覆盖好，不追新

## 入口文档

{{ENTRY_DOCS}}

## 下一步

- 修 bug / 加功能：找到对应目录的 AGENTS.md 或源码入口
- 不确定规则：grep 仓库里已有模式——智能体会复现仓库已有模式，包括坏模式
